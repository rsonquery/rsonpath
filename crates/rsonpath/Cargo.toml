[package]
name = "rsonpath"
version = "0.8.2"
authors = ["Mateusz Gienieczko <mat@gienieczko.com>"]
edition = "2021"
description = "Blazing fast JSONPath CLI tool powered by SIMD"
readme = "../../README.md"
license = "MIT"
keywords = ["json", "jsonpath", "query", "simd", "cli"]
categories = [
    "command-line-utilities",
    "text-processing",
    "parser-implementations",
]
repository = "https://github.com/V0ldek/rsonpath"
rust-version = "1.70"
build = "build.rs"

[[bin]]
bench = false
path = "./src/main.rs"
name = "rq"

[package.metadata.docs.rs]
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
clap = { version = "4.4.4", features = ["derive", "wrap_help"] }
crossbeam-channel = { version = "0.5.8" }
color-eyre = { version = "0.6.2", default-features = false }
eyre = "0.6.8"
log = "0.4.20"
rsonpath-lib = { version = "0.8.2", package = "rsonpath-lib", default-features = false }
simple_logger = "4.2.0"
object-pool = { version = "0.5.4" }

[build-dependencies]
rustflags = { version = "0.1.4" }
vergen = { version = "8.2.5", features = ["cargo", "git", "gitcl", "rustc"] }

[dev-dependencies]
trycmd = "0.14.17"

[features]
default = ["simd", "rsonpath-lib/crossbeam"]
simd = ["rsonpath-lib/simd"]
